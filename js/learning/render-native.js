import { escBidi, esc } from '../html.js';
import { QUIZ_PASS_RATIO } from '../../content/index.js';
import { nahwAnalysisComplete, isWordLesson, isIdeaLesson } from './nahw.js';
import { nativeItem } from './native.js';
import { foundationQuiz } from './nahw-foundation-practice.js';
import { foundationPlan } from './nahw-foundations.js';
import { wordHeaderHtml, wordFooterHtml } from './render-word.js';

export function choiceHtml(options, selected, order, action, disabled) {
  return `<div class="mz-choices" role="group" aria-label="Choose your answer">${order.map((oi, i) => `<button class="mz-choice${selected === oi ? ' is-selected' : ''}" data-action="${action}" data-option="${oi}" aria-pressed="${selected === oi}" ${disabled ? 'disabled' : ''}><span class="mz-choice-letter">${String.fromCharCode(65 + i)}</span><span>${escBidi(options[oi])}</span></button>`).join('')}</div>`;
}

function feedbackHtml({ correct, original, answer, explanation = '', corrected = false, correcting = false, correctionAction, compact = false, footerActions = false, showOriginal = true }) {
  return `<div class="mz-feedback ${correct ? 'is-correct' : 'is-incorrect'}" tabindex="-1" role="status"><h3>${compact && corrected ? 'Correction understood' : correct ? 'Correct' : 'Let’s work through it'}</h3>${explanation ? `<p>${escBidi(explanation)}</p>` : ''}<dl class="mz-answer-comparison${compact ? ' mz-word-answer-comparison' : ''}">${showOriginal ? `<div><dt>${compact ? 'First answer' : 'Your original answer'}</dt><dd>${compact ? original.replace(/^Option /, '') : original}</dd></div>` : ''}${!correct ? `<div><dt>${compact ? 'Expected' : 'Expected answer'}</dt><dd>${compact ? answer.replace(/^Option /, '') : answer}</dd></div>` : ''}</dl>${corrected ? compact ? '' : '<p>Correction understood. Your original attempt is retained.</p>' : !correct && !correcting && !footerActions ? `<button class="btn btn-secondary" data-action="${correctionAction}">Try a correction</button>` : ''}</div>`;
}

export function nativeExerciseHtml(lesson, step, record, order, helpers, state) {
  const analysis = step.kind === 'analysis';
  const item = nativeItem(lesson, step);
  const concept = step.kind === 'check' ? lesson.concepts[step.conceptIndex] : null;
  const disabled = record.submitted && !record.correcting;
  const complete = analysis ? nahwAnalysisComplete(item, record.response) : record.selected != null;
  const courseAnalysis = analysis && !!foundationPlan(lesson)?.analysisLabels;
  const displayedResponse = courseAnalysis && record.corrected && !record.correcting ? record.originalResponse : record.response;
  const roleText = value => foundationPlan(lesson)?.analysisItemLabels?.[item.id]?.[value] || foundationPlan(lesson)?.analysisLabels?.[value] || value;
  const sourceText = value => foundationPlan(lesson)?.analysisText?.[item.id]?.[value] || value;
  const original = record.originalResponse || (record.firstSelected ?? record.selected);
  const comparison = values => item.words.map((word, i) => {
    const text = `${foundationPlan(lesson) ? `${i + 1}. ` : `${escBidi(sourceText(word))} — `}${escBidi(roleText(values?.[i]) || 'No answer')}`;
    return courseAnalysis ? `<span>${text}</span>` : `<div>${text}</div>`;
  }).join(courseAnalysis ? ' · ' : '');
  let controls = analysis ? `<div class="mz-fields">${item.words.map((word, i) => `<div class="mz-field"><label for="native-field-${i}">${escBidi(sourceText(word))}</label><select id="native-field-${i}" data-action="studyField" data-field="${i}" ${disabled ? 'disabled' : ''}><option value="">Choose an answer…</option>${order.map(oi => `<option value="${oi}" ${record.response?.[i] === item.options[oi] ? 'selected' : ''}>${esc(item.options[oi])}</option>`).join('')}</select>${record.response?.[i] ? `<span class="mz-control-help">${escBidi(record.response[i])}</span>` : ''}</div>`).join('')}</div>`
    : choiceHtml(item.options, record.selected, order, 'studyChoice', disabled);
  if (analysis && foundationPlan(lesson)) {
    controls = '<div class="mz-fields" data-part-count="' + item.words.length + '">' + item.words.map((word, i) => '<div class="mz-field"><label for="native-field-' + i + '">' + (i + 1) + '. ' + escBidi(sourceText(word)) + '</label><div class="mz-foundation-select"><span aria-hidden="true">' + escBidi(roleText(displayedResponse?.[i]) || 'Choose an answer…') + '</span><select aria-label="' + esc(sourceText(word)) + '" id="native-field-' + i + '" data-action="studyField" data-field="' + i + '" ' + (disabled ? 'disabled' : '') + '><option value="">Choose an answer…</option>' + order.map(oi => '<option value="' + oi + '" ' + (displayedResponse?.[i] === item.options[oi] ? 'selected' : '') + '>' + esc(roleText(item.options[oi])) + '</option>').join('') + '</select></div></div>').join('') + '</div>';
  }
  const wordHint = ['Grammar studies sounds coined for a meaning.', 'A قول must point to a recognised meaning.', 'Test whether each part carries its own meaning.', 'Ask whether the letters separately mean parts of “man”.', 'Does the phrase make a complete sentence?', 'A combined proper name functions as one unit.'][step.conceptIndex];
  const hintLine = concept?.lines.find(l => ['Definition', 'Rule'].includes(l.box?.title)) || concept?.lines[0];
  return `<section class="mz-exercise mz-nahw-exercise${analysis && foundationPlan(lesson) ? ' mz-foundation-analysis-exercise' : ''}${analysis && foundationPlan(lesson)?.analysisLabels ? ' mz-course-analysis' : ''}${['mizan-sarf', 'mizan-intro-sarf'].includes(lesson.learningModel) ? ' mz-sarf-exercise' : ''}" ${foundationPlan(lesson) ? 'data-foundation-exercise="true"' : ''} data-state="${record.submitted ? 'graded' : 'answering'}"><div class="mz-exercise-prompt"><p class="mz-eyebrow">${step.kind === 'check' ? 'Guided practice' : 'Independent practice'}</p><h2>${escBidi(courseAnalysis && record.submitted ? record.correcting ? (lesson.learningModel === 'mizan-sarf' ? 'Correct each match.' : 'Correct each role.') : 'First answer' : item.prompt)}</h2>${analysis && !isWordLesson(lesson) ? `<div class="mz-stimulus" dir="auto">${escBidi(sourceText(item.source))}</div>${item.translation ? `<p class="mz-control-help">${escBidi(item.translation)}</p>` : ''}<p class="mz-control-help">Choose an answer for every entry, then check your answers.</p>` : item.stimulus ? `<div class="mz-stimulus">${escBidi(item.stimulus).replace(/\n/g, '<br/>')}</div>` : ''}</div><div class="mz-response">${controls}${!disabled && !isIdeaLesson(lesson) ? `<div class="mz-check-row"><button class="btn btn-primary" data-action="studyCheck" ${complete ? '' : 'disabled'}>${record.correcting ? 'Check correction' : 'Check answer'}</button><button class="mz-text-button" data-action="${analysis || concept ? 'studyHint' : 'openStudyNotes'}" ${record.hintShown ? 'disabled' : ''}>${analysis || concept ? 'Hint' : 'Lesson notes'}</button></div>` : ''}${record.hintShown && (!isIdeaLesson(lesson) || !record.submitted) ? `<aside class="mz-hints"><strong>Hint</strong>${analysis ? `<p>${escBidi(item.hint)}</p>` : isIdeaLesson(lesson) ? `<p>${escBidi(item.hint || (isWordLesson(lesson) ? wordHint : 'Identify the grammatical distinction before choosing.'))}</p>` : `<div class="mz-prose">${helpers.prose({ lines: hintLine ? [hintLine] : [] }, '', null, true, state.tarkeebLabelsBlue)}</div>`}</aside>` : ''}${record.submitted ? feedbackHtml({
    correct: analysis ? record.correct : original === item.correct,
    original: analysis ? comparison(original) : isIdeaLesson(lesson) ? `Option ${String.fromCharCode(65 + order.indexOf(original))}` : escBidi(item.options[original]),
    answer: analysis ? comparison(item.labels) : isIdeaLesson(lesson) ? `Option ${String.fromCharCode(65 + order.indexOf(item.correct))}` : escBidi(item.options[item.correct]),
    explanation: analysis ? '' : item.explanations?.[original] || item.explanation || '',
    showOriginal: !(analysis && foundationPlan(lesson)?.analysisLabels),
    corrected: record.corrected, correcting: record.correcting, correctionAction: 'studyCorrect', compact: isIdeaLesson(lesson) && (!analysis || !!foundationPlan(lesson)), footerActions: isIdeaLesson(lesson),
  }) : ''}</div></section>`;
}

export function nativeQuizHtml(state, mod, lesson) {
  if (state.quizShowResult) {
    const score = state.quizScores[mod.id][lesson.id];
    return `<section class="mz-completed"><span class="mz-completed-mark" aria-hidden="true">${state.quizPassed ? '✓' : '↻'}</span><p class="mz-eyebrow">LESSON CHECK</p><h1>${state.quizPassed ? 'Ready to move on' : 'A little more practice'}</h1><p>${escBidi(lesson.title)}</p><div class="mz-result-score"><strong>${score.correct} <small>/ ${score.total}</small></strong><span>correct on the first attempt</span></div><p>${state.quizPassed ? 'You have passed the lesson check.' : `Get at least ${Math.ceil(lesson.quiz.length * QUIZ_PASS_RATIO)} of ${lesson.quiz.length} questions right to finish this lesson. Your practice and notes are saved.`}</p><div class="mz-completed-actions">${state.quizPassed ? '<button class="btn btn-primary" data-action="finishLesson">Finish lesson →</button>' : '<button class="btn btn-primary" data-action="retakeQuiz">Try the lesson check again</button>'}<button class="btn btn-secondary" data-action="backToLesson">Return to the lesson</button></div></section>`;
  }
  const q = foundationQuiz(lesson, state.quizIndex), correction = state.quizCorrection;
  const selected = correction?.active || correction?.correct ? correction.selected : state.quizSelected;
  const disabled = state.quizRevealed && !correction?.active;
  const wordLesson = isIdeaLesson(lesson);
  return `<section class="mz-study ${wordLesson ? 'mz-word-lesson ' : ''}${['mizan-sarf', 'mizan-intro-sarf'].includes(lesson.learningModel) ? 'mz-sarf' : 'mz-nahw'}" ${wordLesson ? 'data-reading-layout="idea"' : ''} aria-label="Lesson check">${wordLesson ? wordHeaderHtml(mod, lesson, state.quizIndex + 1, lesson.quiz.length, true) : `<header class="mz-study-head"><button class="mz-study-exit" data-action="backToLesson" aria-label="Back to the lesson">←</button><div class="mz-study-title"><span>${escBidi(mod.title)}</span><h1>${escBidi(lesson.title)}</h1></div><span class="mz-control-help">Lesson check</span></header><div class="mz-step-track"><span>Independent practice</span><div class="mz-meter" role="progressbar" aria-label="Lesson check progress" aria-valuemin="0" aria-valuemax="${lesson.quiz.length}" aria-valuenow="${state.quizIndex + 1}"><span style="width:${(state.quizIndex + 1) / lesson.quiz.length * 100}%"></span></div></div>`}<div class="mz-study-body" data-study-step ${wordLesson ? 'tabindex="0" role="region" aria-label="Complete exercise"' : ''}><section class="mz-exercise"><div class="mz-exercise-prompt"><p class="mz-eyebrow">Independent practice</p><h2>${escBidi(q.q)}</h2></div><div class="mz-response">${choiceHtml(q.options, selected, state.quizOptionOrder[state.quizIndex], 'selectQuizOption', disabled)}${!disabled && !wordLesson ? `<div class="mz-check-row"><button class="btn btn-primary" data-action="checkLessonQuiz" ${selected == null ? 'disabled' : ''}>${correction?.active ? 'Check correction' : 'Check answer'}</button></div>` : ''}${state.quizRevealed ? feedbackHtml({ correct: state.quizSelected === q.correct, original: wordLesson ? `Option ${String.fromCharCode(65 + state.quizOptionOrder[state.quizIndex].indexOf(state.quizSelected))}` : escBidi(q.options[state.quizSelected]), answer: wordLesson ? `Option ${String.fromCharCode(65 + state.quizOptionOrder[state.quizIndex].indexOf(q.correct))}` : escBidi(q.options[q.correct]), explanation: q.explanation, corrected: correction?.correct, correcting: correction?.active, correctionAction: 'correctLessonQuiz', compact: wordLesson, footerActions: wordLesson }) : ''}</div></section></div>${wordLesson ? wordFooterHtml({ backAction: 'backToLesson', nextAction: 'nextQuizQuestion', nextLabel: state.quizIndex + 1 < lesson.quiz.length ? 'Continue' : 'See results', record: { submitted: state.quizRevealed, correcting: correction?.active, corrected: correction?.correct }, complete: selected != null, correct: state.quizSelected === q.correct, checkAction: 'checkLessonQuiz', correctionAction: 'correctLessonQuiz', message: state.quizRevealed ? 'Your original attempt is retained.' : 'Choose an answer, then check it.' }) : `<footer class="mz-study-foot"><button class="btn btn-ghost" data-action="backToLesson">← Back to the lesson</button><span>${state.quizRevealed ? 'Your original attempt is retained.' : 'Check your answer to continue.'}</span><button class="btn btn-primary" data-action="nextQuizQuestion" ${state.quizRevealed ? '' : 'disabled'}>${state.quizIndex + 1 < lesson.quiz.length ? 'Continue' : 'See results'} →</button></footer>`}</section>`;
}
