# Lesson 13: شبه الجملة by Structure

## What is شبه الجملة?

`شبه الجملة` means an expression that resembles a جملة.

It is not a full `جملة`, but it may stand in a place where a جملة-like meaning is needed. This is especially clear with `الظرف المستقر`, which can take the place of a جملة in certain positions.

> **Example Box**
>
> `الَّذِي عِنْدَكَ`
>
> `الذي` is an اسم موصول and needs a صلة. The expression `عندك` is not a full جملة in outward form, but it can stand in the place required by the صلة.

That resemblance is why it is called `شبه الجملة`.

> **Careful Box**
>
> The word `ظرف` may be used in more than one way:
>
> - `المفعول فيه`
> - words like `فوق`, `تحت`, and `عند`
> - `شبه الجملة`

The intended meaning depends on context.

## Types by structure

By structure, `شبه الجملة` has two types:

| Type | Parts | Example | Translation |
|---|---|---|---|
| الجار والمجرور | حرف جر + مجرور | فِي مَكَّةَ | in Makkah |
| الظرف والمضاف إليه | ظرف + مضاف إليه | عِنْدَ الكَعْبَةِ | near the Ka'bah |

Words like `عند`, `فوق`, `تحت`, and `خلف` are not حروف جر. They are أسماء used as ظروف, and what follows them is مضاف إليه.

## Visual tarkeeb: فِي مَكَّةَ

| فِي | مَكَّةَ |
|---|---|
| جار | مجرور |

Together they form `جار ومجرور`, one type of `شبه الجملة`.

## Visual tarkeeb: عِنْدَ الكَعْبَةِ

| عِنْدَ | الكَعْبَةِ |
|---|---|
| ظرف | مضاف إليه |

Together they form `ظرف ومضاف إليه`, another type of `شبه الجملة`.

## Why جار ومجرور may be called ظرف

Calling a `جار ومجرور` a `ظرف` is a تسامح, a loose usage. `ظرف` belongs properly to زمان and مكان, and حروف such as `لام`, `كاف`, and `رُبَّ` carry no ظرفية at all.

The sense of ظرفية is original in words like `عند` and `فوق` and their sisters. Once that same sense was found in حروف such as `في` and `على`, the name `ظرف` was extended to the whole class of جار ومجرور تغليبًا, letting the dominant case name the group.

Another explanation given is that a `جار ومجرور` needs a `متعلَّق` in the way a `ظرف` needs a `مظروف`. That shared need for something to attach to is enough to justify the shared name.
