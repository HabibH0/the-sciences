# nahw-trainer

**Read `AGENTS.md` before doing any work in this repository.** It is the
single source of truth for how to work here: the three build targets, the
module boundaries, the escaping and bidi rules, save-file compatibility, what
verification means in a repo with no test suite, and how to drive the running
Electron app over CDP without hijacking the user's mouse and keyboard.

This file exists only because Claude Code loads `CLAUDE.md` automatically and
other tools load `AGENTS.md`. Keep the content in `AGENTS.md` — it is the one
that is committed (`CLAUDE.md` is gitignored).
